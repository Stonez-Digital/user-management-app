APP_PORT=8080
JWT_SECRET=replace-with-a-random-secret-at-least-32-characters

# Database: use postgres in production, sqlite for local development
DB_DRIVER=postgres
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=usersdb
DB_SSLMODE=require
DB_PATH=users.db

APP_ENV=production
