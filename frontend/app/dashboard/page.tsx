"use client";
import {useEffect,useState} from "react"; import Link from "next/link"; import {useRouter} from "next/navigation";
type User={id:string;name:string;email:string;role:string;active:boolean}; type Student={id:string;admission_number:string;gender:string;enrollment_status:string;user?:{name:string;email:string}};
async function api(path:string){const t=localStorage.getItem("access_token");const r=await fetch("/backend"+path,{headers:{Authorization:"Bearer "+t}});if(r.status===401)throw Error("Session expired");const d=await r.json();if(!r.ok)throw Error(d?.error?.message||d?.error||"Request failed");return d}
export default function Dashboard(){const router=useRouter();const[users,setUsers]=useState<User[]>([]);const[students,setStudents]=useState<Student[]>([]);const[me,setMe]=useState<User|null>(null);const[error,setError]=useState("");
useEffect(()=>{Promise.all([api("/me"),api("/admin/users"),api("/admin/students")]).then(([m,u,s])=>{setMe(m);setUsers(Array.isArray(u)?u:u?.users||[]);setStudents(Array.isArray(s)?s:s?.students||[])}).catch(e=>{setError(e.message);if(e.message==="Session expired")router.push("/")})},[router]);
function logout(){localStorage.clear();router.push("/")}
return <div className="app-shell"><aside className="sidebar"><div className="logo"><span>S</span><div><strong>Stonez</strong><small>School OS</small></div></div><nav><Link className="active" href="/dashboard">Overview</Link><Link href="/dashboard/students">Students</Link><Link href="/dashboard/enrollments">Enrollment</Link><Link href="/dashboard/attendance">Attendance</Link><Link href="/dashboard/users">Users & Roles</Link><Link href="/dashboard/audit">Audit Logs</Link></nav><div className="sidebar-bottom"><div className="mini-user"><div className="avatar">{me?.name?.[0]||"A"}</div><div><strong>{me?.name||"Administrator"}</strong><small>{me?.role||"school_admin"}</small></div></div><button className="ghost" onClick={logout}>Sign out</button></div></aside><main className="content"><header className="topbar"><div><p className="eyebrow">ADMINISTRATION</p><h1>School overview</h1><p className="muted">A single place to manage your school.</p></div><div className="status"><span/> API connected</div></header>{error&&<div className="error banner">{error}</div>}<section className="stats"><Stat label="Students" value={students.length} detail="Registered profiles"/><Stat label="Users" value={users.length} detail="Accounts"/><Stat label="Active" value={users.filter(u=>u.active).length} detail="Active accounts"/><Stat label="Roles" value={new Set(users.map(u=>u.role)).size} detail="Roles represented"/></section><section className="grid-2"><div className="panel"><div className="panel-head"><div><h2>Recent students</h2><p>Latest student records</p></div><Link href="/dashboard/students">View all →</Link></div><div className="table-wrap"><table><thead><tr><th>Student</th><th>Admission</th><th>Status</th></tr></thead><tbody>{students.slice(0,6).map(s=><tr key={s.id}><td><strong>{s.user?.name||"Student"}</strong><small>{s.user?.email||""}</small></td><td>{s.admission_number}</td><td><span className={"pill "+s.enrollment_status}>{s.enrollment_status}</span></td></tr>)}</tbody></table>{!students.length&&<div className="empty">No students yet.</div>}</div></div><div className="panel"><div className="panel-head"><div><h2>Access snapshot</h2><p>Current role distribution</p></div></div><div className="role-list">{["super_admin","school_admin","teacher","student","parent","accountant","staff"].map(role=><div key={role}><span>{role.replace("_"," ")}</span><strong>{users.filter(u=>u.role===role).length}</strong></div>)}</div></div></section><section className="panel"><div className="panel-head"><div><h2>School modules</h2><p>Foundation ready for the next academic workflows.</p></div></div><div className="module-grid">{["Academic structure","Enrollment","Attendance","Results & report cards"].map((x,i)=><div className="module" key={x}><div className="module-icon">+</div><div><strong>{x}</strong><p>{["Sessions, terms, classes and subjects","Assign students to class and section","Daily attendance tracking","Grades and report cards"][i]}</p></div><span>Next</span></div>)}</div></section></main></div>}
function Stat(p:{label:string;value:number;detail:string}){return <div className="stat"><span>{p.label}</span><strong>{p.value}</strong><small>{p.detail}</small></div>}
