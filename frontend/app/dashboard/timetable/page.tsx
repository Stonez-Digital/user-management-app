"use client";
import{useEffect,useState}from"react";import Link from"next/link";
async function api(path:string){const t=localStorage.getItem("access_token");const r=await fetch("/backend"+path,{headers:{Authorization:"Bearer "+t}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d?.error?.message||"Request failed");return d}
export default function Timetable(){const[x,setX]=useState<any[]>([]),[e,setE]=useState("");useEffect(()=>{api("/admin/timetable").then(d=>setX(Array.isArray(d)?d:d.timetable||d.data||[])).catch(a=>setE(a.message))},[]);return <div className="content standalone"><Link className="back"href="/dashboard/operations">← Operations</Link><header className="topbar"><div><p className="eyebrow">OPERATIONS</p><h1>Timetable</h1><p className="muted">Review the conflict-aware school schedule.</p></div></header>{e&&<div className="error banner">{e}</div>}<section className="panel"><div className="table-wrap"><table><thead><tr><th>Day</th><th>Start</th><th>End</th><th>Room</th><th>Class</th><th>Teacher assignment</th></tr></thead><tbody>{x.map(a=><tr key={a.id}><td>{a.day_of_week}</td><td>{a.start_time}</td><td>{a.end_time}</td><td>{a.room||"—"}</td><td>{a.class?.name||a.class_id||"—"}</td><td>{a.teacher_assignment_id||"—"}</td></tr>)}</tbody></table>{!x.length&&<div className="empty">No timetable entries.</div>}</div></section></div>}
